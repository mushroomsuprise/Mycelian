Legacy Templates:
    [] - Recreate the "memecalc" and "ttimers" in Spore Studio as a trial. Change them to a default "bitcounter" and "subcounter"

Chat Template:
    [] - Add reply options to chat messages. Will need to make this a toggle option in the companion JSON file. Will need to check the message handler to ensure were getting the reply data from Twitch. See image
    [] - Ensure the timestamp feature is working, including the on/off toggle. Add in the ability to format the timestamp and expose as settings in the JSON file.
    [] - Add in the point reward cost to the event message for channel point redeems. Also include the ability to display the channel point icon as seen in the attached image (will likely need to check the EventSub and/or Twitch API to see how this is possible). Make the image a toggle option in the JSON file.
