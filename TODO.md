Legacy Templates:
    [] - Recreate the "memecalc" and "ttimers" in Spore Studio as a trial. Change them to a default "bitcounter" and "subcounter"

Spotify:
    [] - Reduce API rate down to 5 seconds
    [] - Update spotify.html to count the song timer locally, and use the data updates as a "sync" to make sure the song timer numbers and progress bar are still fluid