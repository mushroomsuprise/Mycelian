Legacy Templates:
    [] - Recreate the "memecalc" and "ttimers" in Spore Studio as a trial. Change them to a default "bitcounter" and "subcounter"

UI Updates:
    [] - Overhaul the "Source Settings" UI. Make the preview area able to be hidden. Add in a popped out preview (similar to Spore Studio). Condense top row so the New, Delete, Search box, Reset, and Save elements are in a single row. Move the "select configuration" dropdown and reload buttons to the upper left corner. Remove the "Template Configurations" and the "Manage your temp...." text.
    [] - Limit the height of Connector folder cards to be the same as a single connector, instead of showing every connector. Remove the "Connectors" and "Create trigger-action...." text. Remove the "Help" button and dialog, merge any of its content to the main help system in the app if not already present. Remove the "Examples" button and its related code.
    [] - Remove the top left "Refresh" button from the Giveaways subtab and allow the content to shift up. Remove the title text that says "Giveaways". Remove the Statistics section. Convert the "blocked usernames" to be the same "chip" style setup that the PSN service uses for cached games, move this to the far right edge with the input box at the top and scrollable list of chips underneath. Make the winning announcement text box a normal input box instead of a resizable one. Put the keyword box, winners per draw box, and the winning announcement box on a single row, inline with the blocked username box. Move the Options to its own dialog, add an "Options" button between the "Clear giveaway" and "Refresh" button.
    [] - Update statistics tab UI so main categories can be put in multiple columns, keep the Hype Train and Chatbot stat cards as full width. Remove the "Session Information" card and its contents. Change the "Generate a vibrant highlights...." text to be more descriptive of what this function is.
    [] - Remove the scroll bars from the Theme tab's mock UI (located in the tab areas). Adjust the mock-ui connections card to look like the app footer instead. Move the mock buttons, and saved/info/warning/error elements in this area. Add in missing UI elements
    [] - Adjust App Settings toggle switches so the text is to the right for consistency. Compress the spacing of elements to be tighter. Allow multiple columns for main card categories.
    [] - Update About tab's app data to have the following: Version, Build Number, and Built on date. The build number will need to be generated via the build.py script. Remove the "Open Documentation" button. Add in a button to open the user's browser to the app's github page. Move the "Refresh URLs" button to be in the same row/height as the two text elements above the list of URL cards, keep the right handed justification.
    [] - Make all client ids, client secrets, hosts, ports, passwords, usernames, npsso codes, and urls "hidden" by default. Add in a "visible" button to toggle the display of the actual text. This should be done on the Twitch, OBS, PSN, Spotify, YouTube, and Database subtabs.